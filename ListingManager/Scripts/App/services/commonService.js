(function () {
    'use strict'

    /**
    *@ngdoc function
    *@name ListingManger.service:commonData
    *@descripttion
    * # commonData
    * Service of the Listing Manager
    */
    app.service('commonData', function () {
        //this.checkForDuplicateItems = function contains(list, obj) {
        //    for (var i = 0; i < list.length; i++) {
        //        if (list[i].$$hashKey != undefined)
        //            delete list[i].$$hashKey;

        //        if (list[i].AgentId != undefined)
        //            delete list[i].AgentId;
        //        if (list[i].ListingId != undefined)
        //            delete list[i].ListingId;
        //        if (list[i].OpenHouseId != undefined)
        //            delete list[i].OpenHouseId;

        //        if (obj.AgentId != undefined)
        //            delete list[i].AgentId;
        //        if (obj.ListingId != undefined)
        //            delete list[i].ListingId;
        //        if (obj.OpenHouseId != undefined)
        //            delete list[i].OpenHouseId;

        //        if (JSON.stringify(list[i]) == JSON.stringify(obj)) {
        //            return true;
        //        }
        //    }
        //    return false;
        //}
    });
})();